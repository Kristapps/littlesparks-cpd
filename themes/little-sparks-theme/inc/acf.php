<?php
if ( ! defined( 'ABSPATH' ) ) exit;

// ACF field groups defined in code via acf_add_local_field_group().
// Populated when ACF plugin is installed and configured on staging.
// Fields planned: Trainer (credentials, bio, photo, specialisms, years_experience)
//                 Testimonial (quote, name, role, organisation, photo)
//                 Resource (file, description, category)
